# OpenAPI

Devuelve la especificación OpenAPI generada automáticamente.

```bash
curl "https://api.hola.cloud/openapi.json"
```

La respuesta es un documento JSON que cumple con la especificación OpenAPI.
