# Listar Hosts Virtuales

Devuelve los nombres de hosts virtuales configurados como un arreglo de strings.

```bash
curl -X GET "https://api.hola.cloud/v0/virtualhosts"
```

```json
["mi-proyecto.hola.cloud", "api.mi-proyecto.hola.cloud"]
```
